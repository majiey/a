## 备份

name: '备份博客'

 

on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '5 * * * *'

jobs:

  bot:

    runs-on: ubuntu-latest # 运行环境为最新版的Ubuntu

    steps:

      - name: '访问备份功能的php文件' # 步骤二，执行sh命令文件

        run: curl http://mahaoxuan123.iink.ga/bf.php # 运行命令。（注意，运行目录是仓库根目录）
